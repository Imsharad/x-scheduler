# Example configuration for deploy-aws.sh
# Copy this file to deploy.config and fill in your values.
# DO NOT COMMIT deploy.config WITH SENSITIVE VALUES TO GIT.

# --- AWS Configuration ---
AWS_REGION="us-east-1"        # Your target AWS region
KEY_PAIR_NAME="your-key-pair" # Your existing EC2 Key Pair name
S3_BUCKET_NAME="your-unique-x-scheduler-videos" # Choose a globally unique S3 bucket name
SSM_ENV_PARAM_NAME="/x-scheduler/env" # AWS Parameter Store name holding .env content (SecureString recommended)

# --- Application Configuration ---
GIT_REPO_URL="https://github.com/your-username/your-repo.git" # Your Git repository URL

# --- Optional Overrides (Defaults are in cloudformation.yaml and deploy-aws.sh) ---
# STACK_NAME="x-scheduler-stack"
# EC2_INSTANCE_TYPE="t2.micro"
# DYNAMODB_TABLE_NAME="XSchedulerUserTokens"
# FLASK_PORT=5000
# VPC_ID="" # Specify VPC ID if not using default VPC
# SUBNET_ID="" # Specify Subnet ID if not using default VPC (must be in the specified VPC_ID if provided) 